---
title: 'Users Database'
openapi: 'POST /users'
---

`POST /users`: 

This gets the users of the current room. Requires the auth key as a header. Does NOT use the same backend as the other endpoints, instead uses the waffle-admin subdomain.
